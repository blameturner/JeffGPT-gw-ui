{
  "name": "mst-ag",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["gateway", "frontend"],
  "scripts": {
    "dev:gateway": "npm -w gateway run dev",
    "dev:frontend": "npm -w frontend run dev",
    "build": "npm -w gateway run build && npm -w frontend run build"
  }
}
